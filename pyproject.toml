[project]
name = "geocoordenadas"
version = "0.1.0"
description = "ferramentas cli para a manipulação geográfica"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "folium>=0.20.0",
    "pyproj>=3.7.2",
    "rich>=15.0.0",
    "typer>=0.24.1",
]
